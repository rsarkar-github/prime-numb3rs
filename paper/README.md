# To run a script in the subdirectory **scripts**
- First make sure that you are inside the root level directory of this project.
- Run `python -m paper.scripts.scriptname`.
- The outputs are created in **paper/output/scriptname.txt**

# Documentation of the scripts

`prog1.py`: 